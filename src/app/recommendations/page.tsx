import { ComingOnline } from "@/components/ComingOnline";

export default function RecommendationsPage() {
  return (
    <ComingOnline
      title="Recommendations"
      blurb="Your decision queue: prioritized actions with rationale, evidence, expected impact and effort — approve, edit or dismiss with a reason, and the system learns from the outcome."
      populatedBy="The recommendation engine (guide §10) writes prioritized actions per workspace. Try it live now via a market analysis."
    />
  );
}
