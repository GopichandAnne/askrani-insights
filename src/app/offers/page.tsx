import { ComingOnline } from "@/components/ComingOnline";

export default function OffersPage() {
  return (
    <ComingOnline
      title="Offers & pricing"
      blurb="Structured dish/offer comparison across your competitor set — current price, previous price, validity, source image and confidence, with full price history preserved."
      populatedBy="Extracted offers persist to the offer table (history-preserving); this screen compares them across businesses and over time."
    />
  );
}
